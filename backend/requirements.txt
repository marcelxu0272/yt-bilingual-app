fastapi==0.110.0
uvicorn==0.29.0
youtube-transcript-api==1.2.4
pydantic==2.6.4
openai>=1.0.0
yt-dlp>=2024.0.0
python-dotenv>=1.0.0

# Optional: local ASR fallback for videos without English captions
# pip install faster-whisper  (downloads a Whisper model on first use;
# set WHISPER_MODEL=small/base/medium to choose size, default: base)
